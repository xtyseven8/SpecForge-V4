# {功能名} 功能测试用例

> **文档协议**: 此文档为测试执行前的设计产物，禁止在执行过程中修改（发现的补充场景应记录到"测试用例补充"节末尾）。
> **生成时间**: {YYYY-MM-DD HH:MM}
> **测试人员**: {角色}
> **关联需求**: `specs/features/{功能名}.md`
> **关联技术方案**: `specs/features/{功能名}_技术方案.md`
> **关联代码审查报告**: `docs/开发记录/{功能名}_代码审查报告.md`

---

## 测试目标

本次测试覆盖以下 AC 和关键路径：

- AC-001: {简述}
- AC-002: {简述}
- AC-003: {简述}
- ...

**关键 API 端点**：
- `GET /api/...`
- `POST /api/...`

**关键页面路径**：
- `/` (首页)
- `/search` (搜索页)
- ...

---

## A. 冒烟测试 (Smoke Test)

> 冒烟测试失败时，**立即中止**所有后续测试。

| 用例 ID | 测试目标 | 操作步骤 | 预期结果 | 实际结果 | 状态 |
|---------|---------|---------|---------|---------|------|
| SMOKE-01 | 应用可启动 | 执行 `npm run dev` | 进程启动，无致命错误日志，控制台输出监听端口 | | |
| SMOKE-02 | 首页可访问 | 访问 `http://localhost:{port}` | HTTP 200，页面正常渲染（非白屏），核心元素可见 | | |
| SMOKE-03 | 关键路径走通 | 完成核心流程最简路径 | 全程无 500 错误，无 JS 异常，核心数据可读写 | | |

---

## B. AC 验证测试

> 每条 AC 至少一个测试用例，使用 Given-When-Then 格式。

### AC-001: {AC 描述}

| 用例 ID | Given | When | Then | 实际结果 | 状态 |
|---------|-------|------|------|---------|------|
| AC-001-T1 | {前置条件} | {用户操作} | {预期行为} | | |

### AC-002: {AC 描述}

| 用例 ID | Given | When | Then | 实际结果 | 状态 |
|---------|-------|------|------|---------|------|
| AC-002-T1 | {前置条件} | {用户操作} | {预期行为} | | |

### AC-003: {AC 描述}

| 用例 ID | Given | When | Then | 实际结果 | 状态 |
|---------|-------|------|------|---------|------|
| AC-003-T1 | {前置条件} | {用户操作} | {预期行为} | | |

---

## C. 异常场景测试

> 覆盖边界输入、网络异常、权限边界、空状态等。

| 用例 ID | 测试场景 | 输入/操作 | 预期结果 | 实际结果 | 状态 |
|---------|---------|-----------|---------|---------|------|
| EX-01 | 超长输入 | 输入 10000 字符 | 优雅截断或提示长度限制，不崩溃 | | |
| EX-02 | 特殊字符（XSS） | 输入 `<script>alert(1)</script>` | 正确转义，不执行脚本 | | |
| EX-03 | 网络异常 | 模拟 API 超时/500 | 显示"加载失败，请重试"，不卡死 | | |
| EX-04 | 权限边界 | 未登录访问受保护页面 | 跳转登录页或显示 403 | | |
| EX-05 | 空数据状态 | 查询返回 0 条结果 | 显示"暂无结果"空状态，不显示空白 | | |
| EX-06 | 并发操作 | 快速连续点击按钮 5 次 | 不产生重复数据，有防抖或 loading 状态 | | |

---

## D. 原型一致性测试 (四份文档驱动模式启用时)

> 仅当 `specs/prototype/` 存在且非空时执行。

| 用例 ID | 对照原型文件 | 验证维度 | 预期结果 | 实际结果 | 状态 |
|---------|------------|---------|---------|---------|------|
| PT-01 | {原型文件名}.html | 视觉属性 | 颜色/字体/间距/圆角/阴影与原型一致 | | |
| PT-02 | {原型文件名}.html | 布局结构 | DOM 层级与布局方式与原型对应 | | |
| PT-03 | {原型文件名}.html | 交互行为 | 按钮 hover/active/disabled 状态与原型一致 | | |
| PT-04 | {原型文件名}.html | 响应式 | 断点和布局变化与原型一致 | | |

**截图对照**：
- 原型截图: `specs/prototype/{原型文件名}.html` 渲染截图
- 实现截图: `{功能名}_实现截图.png`
- 差异点: {如有，列出}

---

## 测试用例补充

> 在执行过程中发现的、设计阶段未考虑到的场景，追加到此处。

| 用例 ID | 测试场景 | 输入/操作 | 预期结果 | 实际结果 | 状态 |
|---------|---------|-----------|---------|---------|------|
| SUP-01 | | | | | |

---

## 测试环境

- **应用版本/Commit**: {git commit hash}
- **Node 版本**: {版本}
- **浏览器**: {Chrome/Firefox/Edge + 版本}
- **数据库状态**: {已迁移 / 已加载种子数据}
- **测试工具**: Playwright MCP / curl / 手动

---

## 覆盖度统计

- AC 总数: {N}
- AC 测试用例数: {M}
- AC 覆盖率: {M/N * 100}%
- 异常场景用例数: {K}
- 原型一致性用例数: {L}（四份文档驱动模式启用时）
- **总用例数**: {N+M+K+L}
